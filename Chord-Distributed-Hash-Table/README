Name:     Abhijeet Kulkarni

Programming Language Used: Java

A basic distributed hash table (DHT) with an architecture similar to the Chord 
system. Used Apache Thrift to implement remote procedure calls

Assuming you are in the directory containing this README:

## To clean:
make clean
-------------------------------------------------------------------------------
## To compile:
make
------------------------------------------------------------------------------
## To run:
chmod +x ./server.sh
./server.sh [PORT]

chmod +x ./client.sh
./client.sh [IP] [PORT]
